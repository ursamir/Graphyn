[plugin]
name             = "feature-store-write"
version          = "0.1.0"
description      = "Light feature store write"
author           = "Graphyn Plugins"
platform_version = ">=0.0"
entry_points     = ["types.py", "nodes.py"]
license          = "MIT"

tags = ["mlops", "feature", "stub"]

dependencies = [
    "numpy>=1.24",
]

runtime = "inprocess"
node_types = ["feature_store_write"]

[config_schema.feature_store_write]
stub = { type = "boolean", title = "Stub mode", default = true, description = "When true (default), return typed minimal outputs without heavy ML deps." }
persist_path = { type = "string", title = "Persist path", default = "workspace/artifacts/feature_store", description = "Persist path." }
entity_keys = { type = "array", title = "Entity keys", default = [], description = "Entity keys." }
