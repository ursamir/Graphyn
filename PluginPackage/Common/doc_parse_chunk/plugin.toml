[plugin]
name             = "doc-parse-chunk"
version          = "1.0.0"
description      = "Ingest text/md/html/plain files and split into structure-aware chunks. No vector DB."
author           = "Graphyn Plugins"
platform_version = ">=0.0"
entry_points     = ["types.py", "nodes.py"]
license          = "MIT"

tags = ["document", "chunk", "ingest", "common"]

dependencies = []

optional_dependencies = [
    "unstructured>=0.14",
]

runtime = "inprocess"
node_types = ["doc_parse_chunk"]

[config_schema.doc_parse_chunk]
path = { type = "string", title = "Path", default = "", description = "Path under workspace/datasets/input (or another workspace path)." }
recursive = { type = "boolean", title = "Recursive", default = true, description = "Walk subdirectories." }
max_chars = { type = "integer", title = "Max Chars", default = 1200, description = "Max Chars." }
use_unstructured = { type = "boolean", title = "Use Unstructured", default = false, description = "Enable use unstructured." }
