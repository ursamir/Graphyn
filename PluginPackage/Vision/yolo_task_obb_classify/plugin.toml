[plugin]
name             = "yolo-task-obb-classify"
version          = "0.1.0"
description      = "OBB or classify task wrapper"
author           = "Graphyn Plugins"
platform_version = ">=0.0"
entry_points     = ["types.py", "nodes.py"]
license          = "MIT"

tags = ["vision", "yolo", "stub"]

dependencies = [
    "numpy>=1.24",
]

optional_dependencies = [
    "ultralytics>=8.0",
]

runtime = "inprocess"
node_types = ["yolo_task_obb_classify"]

[config_schema.yolo_task_obb_classify]
stub = { type = "boolean", title = "Stub mode", default = true, description = "When true (default), return typed minimal outputs without heavy ML deps." }
task = { type = "string", title = "Task", default = "obb", description = "Task." }
imgsz = { type = "integer", title = "Imgsz", default = 640, description = "Imgsz." }
