[plugin]
name             = "agent-loop"
version          = "0.1.0"
description      = "Multi-step plan→tool→observe loop"
author           = "Graphyn Plugins"
platform_version = ">=0.0"
entry_points     = ["types.py", "nodes.py"]
license          = "MIT"

tags = ["agents", "agent", "stub"]

dependencies = [
    "numpy>=1.24",
]

optional_dependencies = [
    "httpx>=0.24",
]

runtime = "inprocess"
node_types = ["agent_loop"]

[config_schema.agent_loop]
stub = { type = "boolean", title = "Stub mode", default = true, description = "When true (default), return typed minimal outputs without heavy ML deps." }
max_steps = { type = "integer", title = "Max steps", default = 8, description = "Max steps." }
model = { type = "string", title = "Model", default = "gpt-4o-mini", description = "Model." }
api_secret_name = { type = "string", title = "Api secret name", default = "OPENAI_API_KEY", description = "Api secret name." }
tool_allowlist = { type = "array", title = "Tool allowlist", default = [], description = "Tool allowlist." }
