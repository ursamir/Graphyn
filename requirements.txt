# Deployment pins for the default Graphyn runtime.
#
# AUTHORITATIVE PACKAGE *SET*: setup.py install_requires (+ extras_require).
# This file mirrors that default set with pins suitable for Docker /
# `pip install -r requirements.txt`. Do not add packages here without
# updating setup.py. Validate with:  python scripts/check_deps.py
#
# Clean install (preferred for local/dev):
#   python3 -m venv venv && venv/bin/pip install -U pip && venv/bin/pip install -e ".[dev]"
# Docker / locked runtime:
#   pip install -r requirements.txt && pip install -e . --no-deps

# Core numeric / audio (host plugins + domain)
numpy>=1.26.4,<3
scipy>=1.14.1,<2
librosa==0.10.2.post1
noisereduce==3.0.3
pyloudnorm==0.1.1
soundfile==0.12.1
PyYAML==6.0.2

# API
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
pydantic==2.10.6

# Direct platform imports (were historically missing / drifted)
httpx>=0.27.0,<1
packaging>=23.0

# TOML backport for Python 3.10 only (3.11+ uses stdlib tomllib)
tomli==2.2.1; python_version < "3.11"

# Optional extras are NOT listed here — install via setup.py extras:
#   pip install -e ".[mcp,redis,events,vad,hf,tf,dev]"
